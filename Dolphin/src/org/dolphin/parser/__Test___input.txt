{
{
    var tex;
    tex = background_get_texture(tex_box);
    draw_set_color(c_white);
    d3d_set_projection_ext(x-cos(direction*pi/180)*zoom,y+sin(direction*pi/180)*zoom,50+z,x,y,z+7,0,0,1,
        /*ext: */45,view_wview[view_current]/view_hview[view_current],1,240);
    /*
    d3d_light_define_direction(0,-100,100,1,c_white);
    d3d_light_define_point(1,x,y,z+10,80+random(100),c_yellow);
    d3d_light_enable(0,true);
    d3d_light_enable(1,true)
    */
    d3d_transform_add_rotation_x(xangle);
    d3d_transform_add_rotation_y(yangle);
    d3d_transform_add_translation(x,y,z);
    //The added .5 is because of the scale Game Maker's 3D
            //functions are large, and without delicate percision, 
            // The collision-stuff will look wrong.
   d3d_draw_block(-6.5,-6.5,-0.1,+6.5,+6.5,+8,tex,1,1);
   d3d_transform_set_identity();
   //Draw the floor. d3d_draw_floor() doesn't seem to set the normals right.
   texture_set_repeat(true);
