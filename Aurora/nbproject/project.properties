application.desc=Develop your own games with G-Creator!
application.homepage=http://g-creator.org
application.splash=C:\\Documents and Settings\\Lu\u00eds\\Os meus documentos\\NetBeansProjects\\Aurora\\src\\org\\gcreator\\resources\\GCreator.png
application.title=G-Creator
application.vendor=G-Creator
build.classes.dir=${build.dir}/classes
build.classes.excludes=**/*.java,**/*.form
# This directory is removed when the project is cleaned:
build.dir=build
build.generated.dir=${build.dir}/generated
# Only compile against the classpath explicitly listed here:
build.sysclasspath=ignore
build.test.classes.dir=${build.dir}/test/classes
build.test.results.dir=${build.dir}/test/results
debug.classpath=\
    ${run.classpath}
debug.test.classpath=\
    ${run.test.classpath}
# This directory is removed when the project is cleaned:
dist.dir=dist
dist.jar=${dist.dir}/Aurora.jar
dist.javadoc.dir=${dist.dir}/javadoc
excludes=
includes=**
jar.compress=true
javac.classpath=\
    ${libs.swing-layout.classpath}:\
    ${libs.swing-app-framework.classpath}:\
    ${libs.absolutelayout.classpath}
    

# Space-separated list of extra javac options
javac.compilerargs=
javac.deprecation=false
javac.source=1.5
javac.target=1.5
javac.test.classpath=\
    ${javac.classpath}:\
    ${build.classes.dir}:\
    ${libs.junit.classpath}
javadoc.additionalparam=
javadoc.author=false
javadoc.encoding=${source.encoding}
javadoc.noindex=false
javadoc.nonavbar=false
javadoc.notree=false
javadoc.private=false
javadoc.splitindex=true
javadoc.use=true
javadoc.version=false
javadoc.windowtitle=

jnlp.codebase.type=local
jnlp.codebase.url=file:/C:/Documents%20and%20Settings/Lu\u00eds/Os%20meus%20documentos/NetBeansProjects/Aurora/dist
jnlp.enabled=true
jnlp.icon=C:\\Documents and Settings\\Lu\u00eds\\Os meus documentos\\NetBeansProjects\\Aurora\\src\\org\\gcreator\\resources\\GCreator.png
jnlp.offline-allowed=true
jnlp.signed=true
# Property libs.absolutelayout.classpath is set here just to make sharing of project simpler.
# The library definition has always preference over this property.
libs.absolutelayout.classpath=../../../../../Programas/NetBeans 6.0 beta2/java1/modules/ext/AbsoluteLayout.jar
# Property libs.outlookbar.classpath is set here just to make sharing of project simpler.
# The library definition has always preference over this property.
libs.outlookbar.classpath=../../Desktop/l2fprod-common-7.3/lib/l2fprod-common-outlookbar.jar
# Property libs.swing-app-framework.classpath is set here just to make sharing of project simpler.
# The library definition has always preference over this property.
libs.swing-app-framework.classpath=../../../../../../Program Files/NetBeans 6.0 Beta 2/java1/modules/ext/appframework-1.0.2.jar;../../../../../../Program Files/NetBeans 6.0 Beta 2/java1/modules/ext/swing-worker-1.1.jar
# Property libs.swing-layout.classpath is set here just to make sharing of project simpler.
# The library definition has always preference over this property.
libs.swing-layout.classpath=../../../../Program Files/NetBeans 6.0 Beta 1/platform7/modules/ext/swing-layout-1.0.2.jar
main.class=org.gcreator.core.gcreator
manifest.file=manifest.mf
meta.inf.dir=${src.dir}/META-INF
platform.active=default_platform
run.classpath=\
    ${javac.classpath}:\
    ${build.classes.dir}
# Space-separated list of JVM arguments used when running the project
# (you may also define separate properties like run-sys-prop.name=value instead of -Dname=value
# or test-sys-prop.name=value to set system properties for unit tests):
run.jvmargs=
run.test.classpath=\
    ${javac.test.classpath}:\
    ${build.test.classes.dir}
source.encoding=UTF-8
src.dir=src
test.src.dir=test
